<%- include('partials/header') %>
<body>
    <%- include('partials/navbarhomepage') %>
    <div class="container">
        <div class="mt-5 pb-5 pt-5 px-5 bg-secondary">
            <div class="container">
                <h1 class="display-3 text-light">Bem vindo ao blog</h1>
                <p class="lead text-light">Este é um blog simples feito com NodeJS, Express, EJS, Bootstrap 5 e Mysql</p>
            </div>
        </div>
        <% articles.forEach(function(article){ %>
            <div class="card mt-2">
                <div class="card-header">
                    <%= article.title %>
                </div>
                <div class="card-body">
                    <a href="/article/<%= article.slug %>" class="btn btn-primary">Ler artigo</a>
                </div>
            </div>
        <% }) %>
            <a href="/articles/page/2" style="float:right" class="btn btn-primary m-2">Next</a>
    </div>
</body>
<%- include('partials/footer') %>